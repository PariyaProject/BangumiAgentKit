# PR-7H Recovery Review Readiness

Status: `REVIEW_AUTHORIZED`

## Exact Candidate and remote CI

- Recovery Base: `be89a2699ed7ccc85cf2e23718319bc57e1e16b6`
- Feature branch: `codex/recovery-pr-7h-discovery-renderer`
- Pull request: `#4 — https://github.com/PariyaProject/BangumiAgentKit/pull/4`
- Implementation Candidate SHA:
  `043a5a02cff8e596d435bedd7e0bc37ab8a3ebce`
- Exact mandatory CI run:
  [31493891023](https://github.com/PariyaProject/BangumiAgentKit/actions/runs/31493891023)
- Exact-SHA CI result: `PASS`
- Mandatory jobs passed: `provider-foundation`, `sqlite-default`,
  `host-integration`, `postgres-compat`, `standalone-release-smoke`, and
  `discovery-foundation`.
- Candidate worktree: clean after the implementation commit and before this
  governance-only readiness record.
- `git diff --check`: passed.

The implementation Candidate is the exact code reviewed by local validation
and remote CI. This readiness record is governance metadata and must not be
confused with the Implementation Frozen SHA.

## Product and implementation evidence

The recovery reconstructs the evidence-bearing `discovery-results` renderer
ViewModel/card and `bangumi.render_query_subjects` without changing discovery
query semantics, provider policy, or source boundaries. It preserves the
matched/observed/returned/rendered distinction, exact/estimated/unknown total
semantics, plan filter classifications, field evidence count/source operation,
coverage reasons, bounded warnings/limitations, unknown fields, and degraded
states. Criteria display is bounded per group/value/character, and rendered
items/exported image inputs are capped at 12.

The tool reuses the exact `bangumi.query_subjects` Zod schema and
`DiscoveryEngine`/`ProviderRegistry` seam, is `auth: none` and `risk: read`,
defaults explanation to compact, honors explicit `explain: "none"`, and fails
closed without a ProviderRegistry. No new network call, credential path,
persistence, write authority, migration, HTML/Structured Web source, or PR-7G
behavior was added.

## Local validation

All required validation passed against the Candidate before review readiness:

- `pnpm build`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test` — 33 files, 186 tests
- `pnpm test:contract` — 22 tests
- `pnpm test:semantic` — 31 tests
- `pnpm test:provider` — 33 tests
- `pnpm test:discovery` — 9 files, 51 tests
- `pnpm test:standalone` — 18 tests
- `pnpm test:integration:sqlite` — 12 files, 33 tests
- `pnpm test:render` — 6 files, 49 tests
- `pnpm openapi:verify` — pinned spec validation, generated client/registry,
  build, catalog generation, and exact generated-file diff all passed
- `git diff --check`

The normal security/error regression fixtures emit expected diagnostic output;
PostgreSQL-only integration cases are skipped when `DATABASE_URL` is absent.
Neither condition caused a failure.

## User and Agent QA

- The representative questions in the Cycle Plan were exercised through
  complete, partial, empty, unsupported, unavailable, long-CJK, and maximum
  bounded fixtures.
- Agent QA verified shared schema parity, bounded input rejection for raw
  execution budgets, compact explanation default, explicit `explain: "none"`,
  read/no-auth metadata, ProviderRegistry fail-closed behavior, artifact
  return, and renderer registration.
- Legacy search/discovery/provider/standalone/renderer parity tests remain
  green.

## Renderer and visual QA

The state/input matrix generated deterministic PNGs at both 640px and 960px:

- complete, partial, unsupported, unavailable, empty;
- long CJK title/criteria;
- maximum valid criteria envelope and 12-item renderer cap;
- missing images and missing numeric/date fields.

The full matrix was generated by the focused renderer test. Representative
artifacts were also generated and inspected at:

`/Users/wuzhao/.codex/visualizations/2026/08/11/019ff0d1-278d-78f0-a85c-4e7d82a5d9f6/pr7h-recovery-qa/`

Inspection confirmed readable hierarchy and density at both widths, CJK
wrapping without clipping, explicit unknown values, bounded criteria with
omitted counts, missing-image fallback, and distinct degraded states. The
manifest records all 14 outputs and their dimensions/byte sizes.

## Review gate

- Review Tier: `TIER_2`
- Milestone Sol budget: `2 authorized / 0 consumed / 2 remaining`
- Expected Sol: `1`
- Automatic maximum: `2`
- Reviewer order: sequential `sol_milestone_reviewer` #1; if it returns
  `CORRECTIVE_REQUIRED`, Luna may fix all findings, create a new Candidate,
  obtain exact-SHA CI, and use the same comprehensive reviewer as Sol #2.
- Generic subagents: `0 authorized / 0 consumed`
- Outer self-evolution Sol ledger: `N/A`
- Reviewer reasoning: `high`
- Reviewer overall wall-clock limit: 120 minutes from launch.
- No known P0/P1 blocker is intentionally deferred.

Next action is the single authorized Sol #1 comprehensive review of the
complete Base..Candidate recovery epoch. No Sol reviewer was launched before
this readiness gate.
